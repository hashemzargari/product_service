# Debug settings
debug: true
trace: true

# Database credentials
database:
  driver: pgsql
  host: localhost
  port: 5432
  name: product_service_test
  user: product_service
  password: root

# Logging
log:
  level: debug
  path: /var/www/logs
